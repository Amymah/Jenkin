pipeline {
    agent any

    stages {
        stage('Test Stage') {
            steps {
                echo '✅ Jenkinsfile is being executed!'
                bat 'dir'   // list files in workspace
            }
        }
    }
}
